#include<bits/stdc++.h>
using namespace std;
string b;
int main(){
    freopen("test.txt","r",stdin);
    while(1){
    //cin>>a;
    if(!getline(cin,b))
        break;
    else
    cout<<b.find("//")<<endl;
    //cout<<regex_replace(b,regex("->"), ".")<<endl;
    //str.replace(str.begin()+11,str.end(),str4.begin(),str4.end());
    //replace( b.begin(),b.find("->"), "->", "->");
    }
    return 0;
}


//freopen("test.txt","r",stdin);
    //while(1){
    //cin>>a;
//    if(!getline(cin,b))
    //        break;
    //else
    //cout<<b.find("//")<<endl;
    //cout<<regex_replace(b,regex("->"), ".")<<endl;
    //str.replace(str.begin()+11,str.end(),str4.begin(),str4.end());
    //replace( b.begin(),b.find("->"), "->", "->");
    //}
